// type User = {
//   id: string;
//   name: string;
//   avatar: string;
// }

export interface User { 
    // export because it can be accessed anywhere outside this file
    id: string;
    name: string;
    avatar: string;
}